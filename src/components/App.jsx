import React from 'react';
import ToolbarHeader from './ToolbarHeader';
import GridCards from './GridCards';

const App = () => (
  <div>
    <ToolbarHeader>
      <GridCards />
    </ToolbarHeader>
  </div>
);

export default App;
